<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androlord.prayaas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".Support.AdminBookView" />
        <activity android:name=".Support.AdminPanel" />
        <activity android:name=".Support.ViewPdf" />
        <activity android:name=".Support.AddEbook" />
        <activity android:name=".Support.ChatBox" />
        <activity android:name=".Support.AddBooks" />
        <activity android:name=".Support.BookDetails" />


        <activity android:name=".Support.SplashScreen"
            android:noHistory="true"
            android:theme="@style/AppTheme.NoActionBar"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />


                <category android:name="android.intent.category.LAUNCHER" />


            </intent-filter>

        </activity>
        <activity android:name=".Support.Login" />
        <activity android:name=".MainActivity" />
    </application>


</manifest>